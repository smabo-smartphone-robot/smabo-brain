aiohttp>=3.9

# Image processing (brain.vision). Headless OpenCV avoids GUI deps on SBC/PC.
# Only needed when a vision mode is enabled; brain.vision imports cv2 lazily.
numpy>=1.24
opencv-contrib-python-headless>=4.7

# WebRTC hub (brain.webrtc_hub): brain receives the smabo-app camera as a
# WebRTC peer (low-latency, 30fps) and relays it to smabo-web preview clients.
# aiortc pulls in PyAV (ffmpeg bindings) for video decode. Imported lazily so
# the relay still starts without it (camera/vision simply stay disabled).
aiortc>=1.6

# Speech-to-text (brain.stt): smabo-app sends a recorded utterance on
# /speech/audio and the brain transcribes it to /speech/recognized. Default
# engine is vosk (lightweight, offline). faster-whisper is an optional, more
# accurate alternative — install it only when --stt-engine whisper is used:
#     pip install faster-whisper
# Both are imported lazily, so the relay still starts without them.
# (Some platforms only ship wheels up to 0.3.44 — pin accordingly.)
vosk>=0.3.42
