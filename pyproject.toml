[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "smabo-brain"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["aiohttp>=3.9"]

[project.scripts]
smabo-brain = "brain.__main__:main"
